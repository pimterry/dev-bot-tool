xdescribe("Dev-Bot", () => {
    // TODO
    // Start up a mock AWS server (and stop on teardown)
    // Set that server as our standard proxy with HTTP_PROXY
    // Maybe need to disable SSL with a config file?
    // Make server reply to everything with a 200 by default
    // Build a helper for stubbing given URLs with JSON bodies (reset between tests)
    describe("deploy", () => {
        it("deploys the bundled code to lambda", () => {

        });
    });

    // Test help & version (since we can't unit test them)
});
